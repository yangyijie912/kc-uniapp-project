### 开发日志

**03.09**

- 初始化 uni-app 项目。
- 规划目录结构和基础数据结构，补充 Card 类型定义。
- 完成页面注册与基础路由配置。
- 完成首页第一版布局和样式。

**03.10**

- 完成主要页面骨架和页面跳转：分类页、卡片列表页、卡片详情页、测验页。
- 接入初始卡片数据：将 JSON 数据转换为 Card 类型，并通过 Map 建立分类名到分类 ID 的映射。
- 完成分类主题映射：主题配置放在 config 中，通过固定主题池加稳定映射保证同名分类主题不变。
- 编写文档导入脚本 v1，导入结果先落到 data/cards.json。

**03.11**

- 完成分类管理页面和分类 CRUD 接口，先把分类链路跑通。

**03.12**

- 设计变更：数据库从SQLite改为本地storage存储，主要考虑成本和大小，后续再根据项目情况连接真实数据库或云存档，但是项目结构保留可扩展性。
- 接入本地存储：初始数据从静态文件读取，后续增删改查统一落到 storage。
- 统一接口返回格式：补充 ServiceResult 和 serviceHelper，方便处理成功与失败状态。
- 增加固定的“未分类”：使用固定 ID，且不可编辑、不可删除。
- 实现 useCategoryView：计算分类下卡片数量；未分类在没有卡片时隐藏，同时不展示编辑和删除入口。

**03.13**

- 设计变更：全面移除子类别 subcategory ，简化模型。
- 按同样的存储思路改造 cardService，补充卡片 CRUD。
- 增加分类删除联动：删除分类后，原分类下的卡片自动移入“未分类”。
- 调整数据模型：移除 subcategory 字段以及相关页面和依赖，将细分能力收敛到 tags，并完成数据清洗。
- 补充首页搜索、卡片列表页数据接入和卡片模糊查询。

**03.14**

- 继续完善卡片列表页：补充分类名称展示、状态文案展示，并接通列表页到详情页的真实跳转。
- 调整卡片列表页的页面职责：区分“普通列表页”和“搜索结果页”，搜索入口进入后隐藏本页搜索框和筛选条。
- 修正卡片列表页交互细节：补上输入框双向绑定、状态筛选切换后的样式联动，以及查询参数类型转换。
- 小范围优化 composable：减少 useCategoryView 中的重复计算，并让 useCardListView 中的分类名称映射跟随分类数据自动更新。
- 完善卡片详情页展示：详情页继续保持纯展示，只负责按卡片 ID 读取数据，并补充分类名称显示。

**03.15**

- 完成卡片编辑页的新增和编辑链路：补齐表单校验、分类选择、卡片数据回填，以及新增/更新时的本地存储写入。
- 接通卡片编辑页入口：从列表页进入新增，从详情页进入编辑，卡片 CRUD 页面链路基本跑通。
- 继续梳理卡片服务和分类服务的边界，补充导入分类匹配规则，并把存储 key、未分类信息等重复常量抽到统一文件。
- 修正卡片编辑页的长文本输入问题：补充多行输入框的字数限制配置，保证正文内容可以继续输入。

**03.16**

- 设计变更：content从富文本改成markdown，并简易实现markdown编辑器，主要考虑到富文本编辑器的集成成本较高，而 markdown 既能满足基本的格式需求，又能通过简单的文本输入实现，后续再根据需要迭代更完善的编辑体验。
- 完成卡片删除链路：在卡片编辑页接入删除确认、删除执行和删除后的返回逻辑，补齐卡片 CRUD 闭环。
- 完成 tags 展示收尾：补上列表页和详情页中的 tags 展示与样式调整，让卡片信息层更完整。
- 详情页接入 Markdown 渲染：引入 markdown-it，将卡片 content 从 Markdown 字符串转换后交给 rich-text 展示，并补充标题、列表、引用、代码块、图片等基础样式。
- 继续迭代卡片编辑页的 Markdown 录入体验：补充常用格式插入按钮，并将正文工具区从页面中拆出为独立组件，减少编辑页主文件体积。
- 处理开发期配套问题：补齐 markdown-it 类型声明，完成相关类型检查。

**03.17**

- 优化编辑页的 Markdown 工具栏样式，补充展开和收起。
- 处理 Markdown 编辑区光标插入位置。当前方案不考虑插入以后的光标和聚焦，待后续优化。

**03.18**

- 抽取公共 Markdown 展示组件：将详情页里原本分散的 Markdown 渲染和样式整理到统一组件中。
- 开始搭测验页骨架：补上题目区、答案/内容展开区、进度区和状态按钮的页面结构。

**03.19**

- 继续推进测验页真数据接入：将题目、答案和补充笔记从演示内容切换为当前卡片数据，完成测验页的基本数据流。
- 梳理测验队列的数据结构：明确区分卡片候选列表和当前测验队列，改为使用独立的 cardQueue 保存本轮测验数据，避免直接把计算属性当作队列使用。
- 补上基础抽题逻辑：先按“非掌握”状态筛选候选卡片，加入简单的数量限制，并通过洗牌算法打乱顺序，生成当前测验队列。
- 接通测验状态更新链路：点击“不会 / 模糊 / 掌握”时先调用 updateCard 更新卡片状态，再切换到下一题。

**03.20**

- 完成测验页空状态展示。
- 加上全局样式，统一收掉多页根容器上的 100vh 高度，解决滚动条问题；左侧偶发白边待后续观察/优化。
- 新增测验结果页基础展示：补上结果页页面结构和样式，包括结果头图、总题数/掌握数统计卡、三种状态分布区，以及底部操作按钮。
- 接通测验结果链路：测验完成后写入本轮结果数据，结果页从 storage 读取统计信息展示；同时将结果数据改为响应式对象，确保页面进入时能正确刷新展示。

**03.21**

- 抽离首页测验入口配置弹层：将“开始测验”相关 UI 从首页页面文件中拆出为独立的 QuizSetupSheet 组件，首页只保留弹层开关、开始事件和跳转逻辑。
- 完善测验入口参数链路：首页启动测验时开始透传 mode 和 type 参数，测验页补上参数解析和队列构建前的配置承接，为后续区分自由测验/每日测验做准备。

**03.22**

- 重构测验相关类型quizQuery，优化参数传递实现结果页“再来一轮”，更新测验页面文本
- 补充分类测验入口，优化测验组件的事件传递，统一参数格式为 quizQuery 对象
- 优化测验组件样式，统一颜色变量。优化测验结果页面按钮文本，简化返回操作逻辑

**03.23**

- 继续完善测验入口配置弹层：补上练习数量 limit 的选择区样式，将固定数量选项和自定义输入统一到同一套控件视觉中。
- 接通 limit 的前端状态承接：支持在配置弹层中切换 10 / 20 / 30 和自定义数量，并在开始测验时一并透传到 quizQuery。
- 优化自定义数量输入框的交互反馈：补上聚焦态高亮、蓝色光晕和光标颜色，提升输入时的可感知性。

**03.24**

- 设计变更：每日测验不支持练习模式和自定义数量，逻辑为“按天固定题集 + 中断恢复 + 当日结果记录”，后续根据需要再增加不同模式和数量的每日测验选项。
- 添加每日测验功能，优化测验题目获取逻辑，重构相关类型和工具函数
- 重构测验相关逻辑，优化每日测验进度管理，新增卡片视图转换工具函数
- 调整题目数量限制逻辑和测验弹窗UI，每日测验固定数量，不支持分类和模式

**03.25**

- 优化每日测验题目抽取逻辑，按状态比例分配题目数量，并增强过滤功能
- 修改答案展示逻辑，答案只能查看一次

**03.26**

- 完成简单的json导出（H5端和App端）
- 添加“我的”页面，设计TabBar，移除首页底部的管理模块
- 修复更改TabBar后引起的样式问题，优化最小高度和内边距，设置页面/组件和TabBar的安全距离

**03.27**

- 添加sharp的依赖项并更新标签栏图标为png格式
- 移除一些不必要的变量，调整项目结构

**03.30**

- 修复列表页搜索回到搜索模式的问题，优化搜索功能
- 修复初始数据缺失标签的问题。等待后续导入和迁移统一处理。

**04.01**

- 修复: 更新测验卡片过滤逻辑，允许未定义状态的卡片在复习模式下显示，避免只有初始无状态卡片时没有任务测验卡片。
- 修复：每日测验不允许再来一轮。

**04.04**

- 推进数据导入功能，实现从 JSON 文件读取到数据解析的基础流程，梳理导入整体链路。
- 明确分类导入规则：以分类名称优先匹配，名称一致复用已有分类，名称不一致则创建新分类，并建立导入分类 ID 与系统分类 ID 的映射关系。
- 完成分类映射结构设计（Map），用于在导入卡片时正确转换分类归属，理清导入分类与卡片之间的映射关系，解决“多数据源合并”带来的混乱问题。避免导入后数据错乱。
- 梳理卡片导入策略：导入卡片需先标准化处理，再根据 ID 判断新增或覆盖，保证数据一致性。
- 优化 importService 结构，逐步将导入相关逻辑从 cardService中剥离。
- 修正导入流程中的错误处理逻辑，统一返回 ServiceResult 结构，提升导入过程的稳定性。
- 已完成H5端和App端的JSON导入功能。

**04.05**

- 优化导入结果的用户反馈：在导入完成后展示成功导入的分类数量和卡片数量，提升用户体验。
- 修正导入过程中未分类的处理逻辑，确保无论是通过ID还是名称匹配，导入的未分类都能正确映射到系统的未分类ID，避免数据混乱。
- 完成BaseDialog组件，完善Markdown工具栏的弹层交互。

**04.07**

- 实现卡片列表视图的分页加载，补充新的分页逻辑和类型。
- 修复重新进页面和切后台回来时分页状态被误清空的问题。（关键改动：useCardListView.ts，为了保持分页状态的持久性以及简化分页请求，更改了分页参数的处理方式）
- 修复cardList在H5页面双滚动条问题，调整页面高度计算方式。

**04.08**

- 增加批量更新功能，支持在卡片列表页选择多个卡片进行分类转移，重新调整操作栏样式和交互逻辑。（关键改动：cardList）
- 增加批量删除功能，支持在卡片列表页选择多个卡片进行删除，补充相关接口和交互设计。（关键改动：cardService.ts，增加了两个批量接口批量更新和批量删除）

**04.09**

- 利用 HbuilderX 打包测试（APP端）
- 修复 App 端问题：调整 TabBar 图标尺寸，优化 App 端的导入提示，设定安卓端 targetSdkVersion 暂时为30（安卓11）。

**04.10**

- 更新导入功能，增加导入结果统计，显示新增、跳过和覆盖的分类及卡片数量
- 更新导出功能，更改导出文件名和限制导出文件数量
- 修复导出文件阻塞导出流程的问题，优化旧导出文件清理逻辑，增加错误处理
- 升级 DCloud 依赖，修复运行版本和编译版本不一致的问题

**04.11**

- 修复导出功能中删除文件时文件名不匹配导致无法删除的问题
- 设定安卓端 targetSdkVersion 暂时为31（安卓12），发布1.0.2版本

**04.12**

- 继续修复导出功能中删除文件时失败的问题（关键改动：exportService.ts 中 App 端现在不再调用清理文件的API，而是轮转写入 5 个固定备份槽位。）

**04.14**

- 补充App端输入事件类型，修复App端content字段修改失败的问题
- 优化卡片列表页的长按交互：自定义长按持续时间；引入 touchedCardId 状态，确保长按触发后松手时不会再触发点击事件。

**04.19**

- 修复一系列markdown样式问题：插入缺少换行导致输入体验不佳、分割线样式不对、表格没有滚动导致字体挤压、引用块底部多余空白等。

**04.20**

- 添加导入模式选择功能，新增覆盖导入
- 重新调整导出逻辑：可以自己输入文件名，非必填，不填的话按当前命名规则。导出时看当前记录有没有达到上限（当前设置5个），没满：直接写入这个真实文件名，满了：找到最旧那一个覆盖写入并更新文件名。
- 优化长按，添加移动阈值的判定；

**04.23**

- 修复markdown文本区this.xxx被错误识别为链接的问题；
- 禁用原生的震动，自定义震动反馈；
- 修复导入提示显示不正确的问题，调整部分文案；
- 更换卡片列表的图标，移除不用的图片；
- 调整卡片高度为固定高度（初版，为拖拽排序做准备）。

**04.24**

- 放弃难调的自定义震动，长按震动改为500ms；
- 卡片新增sort字段，新增了排序相关类型，支持按创建时间、更新时间和自定义顺序排序；updatedAt字段在新增时如果没有传就用当前时间戳；放弃旧数据的兼容，快速清洗数据默认走导出再导入的流程（排序逻辑处理待后续）。
- 关键改动：cardService.ts、importService.ts，增加了sort字段和相关逻辑，调整了createdAt和updatedAt的处理方式。顺便修复了导入服务中覆盖数量计算错误和覆盖导入失败用户数据被清空问题。

**04.26**

- 继续完善排序功能：补充卡片排序配置项，调整卡片列表页的排序选项和排序逻辑，增加按分类顺序排序的选项。
- 关键改动：cardService.ts 中的 getCardsByQuery 方法增加了 cardSortConfig 参数，调整了排序逻辑以支持新的排序选项；cardList/index.vue 中增加了排序选项的配置和处理逻辑。
- importService.ts 只进行数据清洗确保卡片和分类有sort，cardSort.ts 进行统一的排序。

**04.27**

- 继续推进卡片列表排序交互：补上排序模式入口、模式文案和操作栏样式，新增拖拽排序按钮与手柄图标，先把拖拽模式的视觉和轻交互骨架搭起来。
- 调整排序模式的适用范围：限制拖拽排序只能在分类列表下使用，全部列表与搜索结果页禁用排序模式，避免跨分类排序带来混乱。
- 重新梳理列表页搜索逻辑：只有首页带 keyword 跳转进来时才进入“搜索结果模式”，列表页内部搜索只作为普通筛选使用。
- 收口列表页筛选交互：搜索、状态筛选和排序切换前统一退出多选和拖拽，默认回到浏览模式，减少模式残留导致的交互冲突。
- 重构卡片列表页的模式状态：将原来的多选模式和拖拽模式布尔状态收敛为单一 interactionMode，统一管理浏览、多选和排序三种状态，简化互斥逻辑。

**04.30**

- 拖拽排序目标：可以边拖边加载。重点解决两个问题：一是没办法获得全部卡片记录最终排序，二是滚动和拖拽冲突的问题。
- 完成卡片列表真实拖拽排序：页面层接入 scrollTop 和手柄触摸事件，拖拽过程支持本地跟手重排，松手后按分类全量顺序落库。
- 抽离拖拽排序逻辑到 useCardSortDrag：把排序模式判断、拖拽会话、命中计算、自动滚动和排序保存集中到独立 composable，减少列表页主文件的控制链复杂度。
- 修复排序接线后的空列表问题：在 cardService 的 getCards 中将 cardSortConfig 从精确过滤字段中拆出，避免排序参数误参与过滤。
- 解决问题一：明确分页与排序保存的边界：保留 useCardListView 当前“已加载前 N 条”的列表模型，并在服务层新增 updateCardOrderInCategory，通过 Move + 分类全量回放重写 sort，避免拿页面局部列表直接覆盖顺序。
- 解决问题二：调整拖拽与滚动冲突处理：保留原生滚动，增加程序自动滚动节流、原生滚动静默窗口和程序滚动回响保护，解决长列表拖拽时手柄、滚动和命中计算互相打架的问题。
- 补充排序链路关键注释和专项设计文档，记录这轮改动的目标、方案选择、取舍和踩坑点，方便后续复盘和继续迭代。

**05.02**

- 排查卡片列表快速下滑时的卡顿和抖动问题，先把普通卡片的触摸监听改成被动监听，并收紧非必要场景下的长按多选触摸状态，减少滚动过程中的手势抢占。
- 进一步确认抖动主要来自分页加载链路：原先的“下一页”请求会被拼成“前 N 条前缀”，导致触底加载时反复重建前半段列表。
- 修正分页语义：`loadCards` 改为按真实页码加载，滚动到底时只追加单页数据；刷新/重载则一次性重建当前已加载页范围，避免列表反复重排。
- 目前卡片列表在数据未加载完时的快速下滑已经明显缓解，全部数据加载完后滚动表现稳定。
- 继续重整卡片列表页：把单一 `interactionMode` 抽到独立类型文件并作为页面唯一模式源，多选逻辑收进 `useCardSelection`，排序逻辑继续由 `useCardSortDrag` 消费同一模式。
- 将卡片列表展示、批量操作和转移弹窗拆成独立组件，页面只保留编排和业务落库，避免把选择/排序同步成一组隐式协议。

**05.03**

- 为了拖拽手感重新调整拖拽链路。
- 定位并修复卡片列表拖拽排序的顶边上拖死锁问题：自动滚动不再早退中断同帧命中，本地重排和占位刷新继续按当前触点推进；同时把上拖/下拖命中边界改成方向感阈值，减少顶边附近踩线抖动。
- 新增独立排障文档 [card-list-sort-drag-debug.md](./card-list-sort-drag-debug.md)，把这次“鬼打墙”的现象、根因、修复方式和验证步骤单独记下来，方便后续复盘追溯。
- 再补一份完整记录 [card-list-sort-drag-full-record.md](./card-list-sort-drag-full-record.md)，把从“拖拽手感改造”到“顶边上拖死锁”的整条链路按时间线写清楚，避免以后只剩摘要没有过程。
- 额外修正 App 端拖拽刚开始时的空白态：只有拖拽代理样式就绪后才隐藏 active card，避免代理晚一拍时页面先空掉。
- App 端列表层再做一次保守降级：原生端不走 `transition-group`，改成普通 `view + v-for`，避免拖拽或分页更新时把整组节点短暂渲染成空白。
- App 平台判断改成运行时系统信息读取，不再依赖编译宏，避免降级分支在原生端没真正生效。
- 继续修复 App 端触摸链路、分页追加、代理卡、显式占位状态和尾卡落点等问题，具体见 [card-list-sort-drag-full-record.md](./card-list-sort-drag-full-record.md)和 [card-list-sort-drag-app-notes.md](./card-list-sort-drag-app-notes.md)。
